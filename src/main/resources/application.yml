spring:
  application:
    name: DBS Loyalty
  profiles:
    active: dev
#    active: vps, cdnjs, min
deploy:
  # suffix link for CSS and JS
  id: 1
server:
  port: 8181
  servlet:
    context-path: /loyalty